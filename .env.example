# MySQL Configuration
# These are the default values used by the application
# Copy this file to .env and modify if your MySQL setup is different

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_PORT=3306
